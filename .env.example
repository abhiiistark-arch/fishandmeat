# Fish and Meat — environment
# Copy to .env and fill in values. Never commit your real .env file.

# Admin portal — staff logins are created by Super Admin in the Staff module.
# Bootstrap Super Admin (auto-seeded): username abhi / password abhi123
# ADMIN_PASSWORD remains a recovery fallback for username "abhi" or "admin".
ADMIN_PASSWORD=change-me

# Flask session secret — change in production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-long-random-string

# Production hardening
# FAM_ENV=production
# FLASK_DEBUG=0
# SESSION_COOKIE_SECURE=true
# Optional allow-list for mobile APK origins (comma-separated)
# FAM_MOBILE_ORIGINS=https://your-domain.com,capacitor://localhost

# Optional MongoDB Atlas — leave blank to use local JSON in /data
# Required on Vercel (local JSON cannot persist on serverless).
# Example: mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
# IMPORTANT: URL-encode special characters in the password, e.g. ! @ # -> %21 %40 %23
MONGO_URI=

# Database name on Atlas
MONGO_DB_NAME=fishandmeat

# --- Server install (MongoDB on the same machine) ---
# Prefer a dedicated file so DB secrets stay out of the main .env:
#   1. Copy mongo.env.example → mongo.env  (or .mongo.env)
#   2. Set MONGO_URI=mongodb://127.0.0.1:27017
#      or mongodb://user:pass@127.0.0.1:27017/fishandmeat?authSource=admin
#   3. Keep mongo.env / .mongo.env gitignored — never commit real URIs
# App loads .env then mongo.env securely from the server filesystem only
# (not exposed via HTTP / static routes).

# On Vercel also set:
# SESSION_COOKIE_SECURE=true
# (cookies are forced secure automatically when VERCEL=1)
